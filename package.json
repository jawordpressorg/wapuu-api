{
  "name": "wapuu-api",
  "private": "true",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "GPLv2",
  "dependencies": {
    "comma-separated-values": "^3.6.4"
  }
}
